# GEAR GUARD GARY — backend dependencies.
#
# Deliberately short. This service reads two keyless public HTTP endpoints and
# serves JSON; it has no database, no wallet library, no signing stack and no
# chain client, because it never writes anything anywhere.
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7,<3.0
pydantic-settings>=2.3
httpx>=0.27

# Dev / test
pytest>=8.0
